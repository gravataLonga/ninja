// `||` does not short-circuit — the right operand is always evaluated even when the left is true.
var called = false;
var sideEffect = function() {
    called = true;
    return false;
};

var result = true || sideEffect();

return called;
